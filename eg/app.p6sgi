use v6;

sub (%env) {
    return (
        200,
        [ 
            Content-Type => 'text/plain', 
            Content-Length => %env.perl.bytes,
        ],
        [ %env.perl ]
    );
}
# vim: ft=perl6
